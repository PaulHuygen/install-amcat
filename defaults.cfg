AMCAT_USER=${AMCAT_USER:-amcat}
AMCAT_SOCKET=${AMCAT_SOCKET:-/home/amcat}
AMCAT_ROOT=${AMCAT_ROOT:-$AMCAT_SOCKET/amcat}
AMCAT_DB_HOST=${AMCAT_DB_HOST:-localhost}
AMCAT_DB_NAME=${AMCAT_DB_NAME:-amcat}
AMCAT_DB_USER=${AMCAT_DB_USER:-$AMCAT_USER}
AMCAT_DB_PASSWORD=${AMCAT_DB_PASSWORD:-amcat}
AMCAT_LOGDIR=${AMCAT_LOGDIR:-/var/log/amcat}
SERVER_NAME=${SERVER_NAME:-localhost}
UWSGI_SOCKET=/tmp/amcat.socket
NGINX_UWSGI_SOCKET=unix:${UWSGI_SOCKET}
REMOTE_AMCAT_REPO=${REMOTE_AMCAT_REPO:-https://github.com/amcat/amcat.git}
AMCAT_GITBRANCHE=${REMOTE_AMCAT_REPO:-release-3.4}
APT_KEY=${APT_KEY:-E084DAB9}
POSTGRESUSER=${POSTGRESUSER:-amcat}
